name: "ffs/ubuntu/21.10"
script:
- >-
  apt-get update && apt-get -y install python3-yaml python3-jinja2 wget xz-utils make gcc g++ rsync texinfo file m4 bzip2
- export CLFS=${HOME}
- install -d ${CLFS}/sources
- bin/sourcer fetch && bin/builder cross_tools && bin/builder tools
environment:
- SHELL=/bin/bash
- LUET_NOLOCK=true
- LUET_YES=true
image: ubuntu/21.10
source: https://code.funtoo.org/bitbucket/scm/core/ffs.git
type: lxd
directory: /
